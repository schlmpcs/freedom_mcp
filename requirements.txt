mcp>=1.2.0
requests>=2.31.0
python-dotenv>=1.0.0
cryptography>=42.0.0
websockets>=12.0
python-telegram-bot[job-queue]>=21,<22
anthropic>=0.40.0
# Optional: only needed for the agent's claude_code backend (run on a Claude
# Max/Pro subscription instead of an API key). Safe to omit if you use AGENT_BACKEND=api.
claude-agent-sdk>=0.2.0
